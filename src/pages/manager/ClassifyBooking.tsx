import React from 'react';
import LabOrder from '../../features/manager/LabOrder';

const ClassifyBooking: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <LabOrder />
    </div>
  );
};

export default ClassifyBooking;