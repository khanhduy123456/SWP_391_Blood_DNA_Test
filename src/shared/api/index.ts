export { default as apiClient } from './client';
export type { ApiResponse, ApiError } from './types';
export { handleApiError } from './utils/handleError';
// import { apiClient, ApiResponse, ApiError, handleApiError } from '@/shared/api'; (nếu dùng ở nơi khác)